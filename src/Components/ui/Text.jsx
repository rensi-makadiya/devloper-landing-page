
export const GradientText = ({ children, variant = 'pink' }) => {
  return <span className={`gradient-text gradient-${variant}`}>{children}</span>
}

export const Heading = ({ children, size = 'large', className = '' }) => {
  return <h1 className={`heading-${size} heading ${className}`}>{children}</h1>
}

export const Text  = ({ children, variant = 'desc', className = '' }) => {
  return <h1 className={`text-${variant} heading  ${className}`}>{children}</h1>
}

export const SliderText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`slidertext-${variant} heading  ${className}`}>{children}</h1>
}

export const DevlopText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`devloptext-${variant} heading  ${className}`}>{children}</h1>
}

export const CustomerText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`customertext-${variant} heading  ${className}`}>{children}</h1>
}

export const CaseText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`casetext-${variant} heading  ${className}`}>{children}</h1>
}

export const SoftwareText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`Softwaretext-${variant} heading  ${className}`}>{children}</h1>
}

export const ApproachText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`Approachtext-${variant} heading  ${className}`}>{children}</h1>
}

export const AlcalineText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`Alcalinetext-${variant} heading  ${className}`}>{children}</h1>
}

export const FeatureText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`FeatureText-${variant} heading  ${className}`}>{children}</h1>
}

export const HireText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`HireText-${variant} heading  ${className}`}>{children}</h1>
}

export const FootorText  = ({ children, variant = 'title', className = '' }) => {
  return <h1 className={`FootorText-${variant} heading  ${className}`}>{children}</h1>
}




 